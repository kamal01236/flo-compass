# Local Tier 2 smoke test only — production deploy uses GitHub Actions + Fly.io
services:
  flo-compass:
    build:
      context: .
      dockerfile: docker/Dockerfile.local
    ports:
      - "${DOCKER_PORT:-8080}:80"
    restart: unless-stopped

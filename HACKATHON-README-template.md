````md
# The Waters of Innovation  
## Episode 2: The Rapids  
### Cursor Hackathon Team README

## Team

**Team name:** `<team name>`

| Name | Role / Focus |
|---|---|
| `<name>` | `<frontend / backend / AI / UX / testing / demo / other>` |
| `<name>` | `<frontend / backend / AI / UX / testing / demo / other>` |
| `<name>` | `<frontend / backend / AI / UX / testing / demo / other>` |

---

## 1. Concept

**App name:** `<app name>`

**One-sentence pitch:**  
`<what did you build, for whom, and why does it matter?>`

**Target users:**  
`<attendees / speakers / organizers / sponsors / Nagarro teams / guests>`

**Problem solved:**  
`<what event-experience gap does this address?>`

**Why this is not a clone or another app:**  
`<what existing event-platform functionality did you avoid, and what extra value do you add?>`

---

## 2. Demo

**Demo scenario:**  
`<briefly describe the main demo flow>`

**Demo steps:**

1. `<step 1>`
2. `<step 2>`
3. `<step 3>`
4. `<step 4>`

**Demo data / credentials:**  
`<test users, sample data, access notes. Do not include real secrets.>`

---

## 3. Run the App

**Prerequisites:**

```bash
<insert commands>
````

**Install:**

```bash
<insert command>
```

**Run locally:**

```bash
<insert command>
```

**Test:**

```bash
<insert command>
```

**Build:**

```bash
<insert command>
```

**Deployment / demo link:**
`<link or notes>`

---

## 4. Architecture

**Short architecture summary:**
`<explain the main parts in a few sentences>`

```text
User
  |
App
  |
API / Services
  |
Data / AI / External Systems
```

| Component     | Purpose          |
| ------------- | ---------------- |
| `<component>` | `<what it does>` |
| `<component>` | `<what it does>` |
| `<component>` | `<what it does>` |

**Key decisions:**

1. `<decision + reason>`
2. `<decision + reason>`
3. `<decision + reason>`

**Assumptions / limitations:**

1. `<assumption or limitation>`
2. `<assumption or limitation>`
3. `<assumption or limitation>`

---

## 5. Cursor Usage

This section matters for judging.

**How we prepared Cursor during the 7-day setup phase:**
`<rules, repo context, docs, prompt patterns, testing approach, architecture notes>`

**Canonical catalog:** [hackathon-docs/cursor-rules/README.md](hackathon-docs/cursor-rules/README.md) — categorized index, per-rule summaries, and `@cite` guidance.

**Cursor rules / setup files (`<N>` total):**

| File / Rule           | Purpose     |
| --------------------- | ----------- |
| `<.cursor/rules/...>` | `<purpose>` |

Maintain the catalog when adding rules — see `.cursor/rules/cursor-rules-catalog.mdc`.

**How we used Cursor:**
`<planning, coding, debugging, refactoring, testing, UI polish, documentation>`

**Best Cursor-assisted moment:**
`<where Cursor clearly accelerated the team>`

**Where we corrected Cursor:**
`<one example where Cursor was wrong, incomplete, risky, or low-quality, and how you fixed it>`

---

## 6. Speed

**What helped us move fast:**

1. `<example>`
2. `<example>`
3. `<example>`

**What slowed us down:**

1. `<issue>`
2. `<issue>`

**What we would improve next time:**

1. `<improvement>`
2. `<improvement>`

---

## 7. Quality

**Tested flows:**

1. `<flow>`
2. `<flow>`
3. `<flow>`

**Automated tests:**
`<yes/no + short explanation>`

**Manual validation:**
`<how did you check the app?>`

**Security / privacy notes:**
`<data handling, secrets, privacy, access assumptions>`

Do not commit real secrets, tokens, production credentials, or personal attendee data.

---

## 8. Evaluation Mapping

| Evaluation Area    | Evidence                                           |
| ------------------ | -------------------------------------------------- |
| Speed of execution | `<how you moved fast>`                             |
| Cursor utilization | `<how Cursor was used meaningfully>`               |
| Product value      | `<why this matters>`                  |
| Execution quality  | `<working functionality, architecture, stability>` |
| Polish and demo    | `<UX, visual quality, demo readiness>`             |

---

## 9. Final Pitch

**30-second pitch:**
`<short pitch>`

**If we had more time:**

1. `<next feature>`
2. `<next feature>`
3. `<next feature>`

**Production readiness:**
`<what would be needed to make this a real, productive app?>`

```
```

/// Layout tokens for session detail scroll insets and chrome.
abstract final class SessionDetailLayout {
  static const double narrowBreakpoint = 900;

  /// Extra scroll padding so the last list items clear sticky chrome comfortably.
  static const double narrowTabBottomPadding = 24;

  static const double wideTabBottomPadding = 20;
}

# Legacy Azure ARM template

This file targeted Azure Container Apps for the former GitLab CI path.

**Not used** by the current production path (GitHub Actions → GHCR → Fly.io). Kept only for historical reference; do not wire new deploys to it.

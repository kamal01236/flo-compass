# Sample plan with serial-merge patch notes for lifecycle tests.

## Ownership

- Branch: feat/test-plan

### Serial-merge patch notes

- Add route `/foo` to `lib/routing/app_router.dart` on integration only
- Register `FooProvider` in `lib/app.dart` on integration only

## Scope

Test fixture only.
